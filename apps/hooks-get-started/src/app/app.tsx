import React from 'react';
import './app.scss';

export const App = () => {
  const title = 'Hooks get started';
  return (
    <div className="app">
      <h1>{title}</h1>
    </div>
  );
};

export default App;
